#ifndef INTERFACE_H
#define INTERFACE_H
/**
 * @brief Creates and initialisize graphic interface
 * @param[in] list list of point
 * @return int return a value of exit code
 */
int interface_lancer(PointsList * list);

#endif
